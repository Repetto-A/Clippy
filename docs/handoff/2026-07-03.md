# Handoff — 2026-07-03

Estado del loop autonomo al cierre de esta sesion.

## Completado

| Rep | Que |
| --- | --- |
| Docs | `autonomy-charter.md`, ADRs 0001-0007, roadmap Q3 |
| R1 | Timeline semantico (API + UI heatmap) |
| R2 | Explicabilidad hook/razon/rubrica en cards y editor |
| R4 | Import performance JSON/CSV + reporte correlacion |
| R6 | Perfiles `training`/`podcast`/`stream` |
| R3 | Export polish: nombres legibles, preview tabs, badge render |

**Tests:** 73 passing (`pytest -q`).

## Bloqueado (requiere Ale)

- **R0** — Etiquetar una clase (`labels.json`) para baseline M1 real.
- **RC1** — E2E clase 93 min: no hay `.mp4` ni `work/` local en el repo.
- **R7** — Perfil podcast completo (reframe speaker-switch).

## Proximo rep sugerido

1. **R5** — Solo despues de R0 + metricas reales importadas (R4 ya listo).
2. **RC1** — Cuando el video este en disco: `POST /api/jobs/from-path` o upload UI.
3. Pulir **RC2** continuo (lint, docs README export section).

## Archivos clave nuevos

- `src/video_clipper/timeline.py`
- `src/video_clipper/performance.py`
- `src/video_clipper/content_profile.py`
- `src/video_clipper/export_names.py`

## Git

Cambios **sin commit** en working tree. Pedir commit/push explicito a Ale.
